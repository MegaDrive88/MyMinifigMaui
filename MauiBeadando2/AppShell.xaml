<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiBeadando2.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiBeadando2"
    xmlns:pages="clr-namespace:MauiBeadando2.Pages"
    Shell.FlyoutBehavior="Disabled"
    Title="MauiBeadando2">

    <ShellContent
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <ShellContent
        ContentTemplate="{DataTemplate pages:MinifigBuilderPage}"
        Route="MinifigBuilderPage" />

    <ShellContent
        ContentTemplate="{DataTemplate pages:PartSelectorPage}"
        Route="PartSelectorPage" />
</Shell>
